# Product Checker API Configuration
# Copy this file to .env and fill in the values

# Server Configuration
PORT=3003

# Proxy Configuration (Required for accessing protected sites)
USE_PROXY=true
PROXY_SERVER=pr.oxylabs.io:7777
PROXY_USERNAME=customer-mcgrocer_XuAwq-cc-GB
PROXY_PASSWORD=your_proxy_password_here

# Browser Configuration
# IMPORTANT: HEADLESS must be set to false for optimal extraction
# Headed mode (with visible browsers on virtual display) significantly improves
# extraction success rates for vendors like Boots, Superdrug, and others.
# Setting this to true will cause many products to return "Unknown" availability.
HEADLESS=false

# Display for headed mode (Xvfb virtual display)
DISPLAY=:99

# Browser Pool Size (number of concurrent browser instances)
BROWSER_POOL_SIZE=6

# Chrome/Chromium Path (auto-detected if not set)
# CHROME_PATH=/root/.cache/ms-playwright/chromium-1200/chrome-linux64/chrome

# Google Gemini API Keys (for AI extraction fallback)
GOOGLE_GENERATIVE_AI_API_KEY=your_gemini_api_key_here
GOOGLE_API_KEY=your_gemini_api_key_here

# ==========================================
# CRITICAL CONFIGURATION NOTES
# ==========================================
#
# 1. HEADLESS=false is REQUIRED for production use
#    - Headed mode provides 57% vendor success rate
#    - Headless mode only provides 43% vendor success rate
#    - Boots and Superdrug CSS extractors fail in headless mode
#
# 2. Always run with start.sh script
#    - Ensures Xvfb virtual display is running
#    - Automatically enforces HEADLESS=false
#
# 3. Proxy is required for most vendors
#    - Boots, Harrods, and others block non-residential IPs
#    - Without proxy, expect 403 Forbidden errors
#
# ==========================================
